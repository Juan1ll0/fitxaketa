# 003.8 - Mejoras del Dashboard: Alta Manual y Rediseño de la Pantalla Fichar

**Status:** done
**Creada:** 2026-06-23
**Autor:** sistema
**Depende de:** [003.2](./003.2-tab-fichar.md), [003.6](./003.6-paginacion-estadisticas.md), [003.7](./003.7-mejoras-historial.md)
**Spec padre:** [003](./003-dashboard-componentes.md)

## Resumen

Mejorar la pantalla principal (Fichar, `/`) en dos frentes:

1. **Alta manual de fichajes**: un acceso secundario (enlace/botón discreto) que abre una **modal** para registrar una jornada a mano (fecha, hora de inicio, hora de fin), con la opción de **encadenar varias altas** sin cerrar la modal.
2. **Rediseño visual del dashboard**: añadir la **hora actual** junto a la fecha, dar estilo propio al **cronómetro**, a la **tarjeta de estado** y al **resumen de jornadas de hoy**, e introducir pequeños widgets que aporten contexto de un vistazo.

La acción primaria sigue siendo **arrancar/parar** el fichaje con el cronómetro: el alta manual es claramente secundaria y nunca debe competir visualmente con el botón principal.

## Contexto

La spec 003.2 dejó la pantalla Fichar funcional pero con un diseño mínimo: fecha en texto plano, cronómetro `text-6xl` sin envoltorio, estado como simple `<span>` y resumen del día en una línea de texto muted (ver [`src/routes/+page.svelte`](../../src/routes/+page.svelte)).

La spec 003.7 decidió explícitamente que el **alta manual vive en la página principal (Fichar)**, no en el Historial, que queda como vista de solo lectura. Esta spec implementa esa decisión.

El proyecto es **mobile-first**, con tema oscuro (superficies `--color-surface` / `--color-surface-light`, acento `--color-primary` azul). Cualquier widget nuevo debe caber cómodamente en una pantalla de móvil sin scroll excesivo y respetar la jerarquía: cronómetro + botón como protagonistas.

## Decisiones de UX (cerradas por el usuario)

- **Alta manual = función secundaria.** Acceso discreto (enlace/botón pequeño, icono "+"), nunca un botón grande que rivalice con Fichar entrada/salida.
- **Modal para el alta**, con campos: **fecha**, **hora de inicio** y **hora de fin**.
- **Encadenar altas:** tras guardar una jornada, la modal ofrece "Guardar y añadir otra" para seguir metiendo fichajes sin cerrarse.
- **La acción primaria sigue siendo arrancar/parar** el cronómetro; el rediseño no puede degradar esa jerarquía.
- **Mostrar la hora actual** además de la fecha.
- **Dar estilo** al cronómetro, al resumen de jornadas de hoy y al indicador de estado.
- **Alcance del rediseño: "dashboard completo".** Entra todo el bloque mínimo con estilo (A1/B1/C1/D1) **más** el anillo de progreso al objetivo diario (B2/E1), la lista de jornadas de hoy (D2/D3) y al menos un widget de contexto (mini-semana E3 y/o aviso de jornada sin cerrar E4).
- **Jornada mínima en alta manual: bloquear.** Si la duración no alcanza `min_jornada_minutos`, no se permite guardar (mismo rigor que el fichaje normal).

## Decisión de diseño (cerrada por el usuario)

Combinación elegida para este corte:

| Bloque                 | Opción elegida | Descripción                                                                |
| ---------------------- | -------------- | -------------------------------------------------------------------------- |
| A. Cabecera fecha+hora | **A1**         | Fecha larga + reloj `HH:MM` grande debajo (segundos atenuados).            |
| B. Cronómetro          | **B1**         | Tarjeta central con acento de color según estado (Trabajando/Descansando). |
| C. Indicador de estado | **C1**         | Pill con punto de color (verde "Trabajando" / neutro "Descansando").       |
| D. Resumen de hoy      | **D1**         | Fila de stat cards (Hoy, Jornadas y Restante).                             |
| E. Widget de contexto  | **E4**         | Aviso de jornada sin cerrar de un día anterior (banner discreto).          |

> **Nota de alcance:** se elige **B1** (no el anillo B2) y **E4** como único widget de contexto inicial. Esto ajusta el "dashboard completo" de las decisiones de UX: el anillo de objetivo (B2/E1) y la mini-semana (E3) quedan **fuera de este corte**, pero el dashboard debe quedar **preparado para incorporarlos** vía el scaffold de widgets configurables (ver siguiente sección).

### Configurabilidad futura de la pantalla (scaffold, NO implementar ahora)

A futuro se quiere que el usuario pueda **personalizar el dashboard** intercambiando "plugins"/widgets: p. ej. sustituir **E4 por E3**, **B1 por B2**, reordenar o activar/desactivar bloques. Esto **no se implementa en esta spec**, pero la estructura de componentes debe dejarse **preparada (scaffold)** para ello:

- Modelar cada bloque (cabecera, cronómetro, estado, resumen, widget de contexto) como un **componente intercambiable** con una interfaz/props homogénea (`{ jornadas, resumen, settings, ... }`), de modo que añadir una variante (B2, E3) sea registrar otro componente, no reescribir la página.
- La pantalla principal debe **componer una lista/registro de slots** (orden + variante elegida) en lugar de tener el layout "cableado". Por ahora ese registro es **estático** (A1, B1, C1, D1, E4 fijos), pero su forma debe ser la que en el futuro alimentará la configuración del usuario.
- No se crea UI de configuración ni se persiste preferencia alguna en esta spec; solo se deja la **forma del código** lista para ello.
- Ver criterio de aceptación AC-15.

## Propuestas de diseño (catálogo de variantes)

Catálogo completo de variantes consideradas. La combinación cerrada está en la sección anterior; el resto quedan documentadas como variantes futuras (algunas alimentarán la configurabilidad).

### A. Cabecera: fecha + hora (NUEVO)

- **A1 (recomendada):** Línea superior con fecha larga ("Lunes, 23 de junio de 2026") y, debajo o al lado, la **hora actual** en grande tipo reloj (`HH:MM`, con segundos opcionales atenuados). La hora se actualiza cada segundo/minuto.
- **A2:** Hora integrada en el mismo renglón que la fecha, separada por "·" ("Lun 23 jun · 14:32").
- **A3:** Reloj como widget propio (tarjeta) arriba del todo.

Criterio: la hora no debe confundirse con el cronómetro (que es HH:MM:SS de tiempo fichado). Diferenciar por tamaño/posición/etiqueta.

### B. Cronómetro (REDISEÑO)

- **B1 (recomendada):** Envolver el cronómetro en una **tarjeta/anillo central** (card con `--color-surface-light`, borde sutil, esquinas redondeadas) que cambia de color de acento según el estado: verde/primario cuando "Trabajando", neutro cuando "Descansando". Mantener `font-mono tabular-nums`.
- **B2:** **Anillo de progreso** (SVG circular) alrededor del cronómetro que representa el avance hacia el objetivo diario de horas (derivado de `horas_semanales / dias_laborables`). El anillo se llena conforme avanza la jornada; al llegar al objetivo, cambia a verde.
- **B3:** Mínimo: solo mejorar tipografía y añadir un punto de estado parpadeante.

Criterio: B2 aporta mucho valor (feedback de objetivo) pero requiere el cálculo de objetivo diario; puede fasearse (B1 primero, B2 después).

### C. Indicador de estado (REDISEÑO)

- **C1 (recomendada):** **Badge/pill** con punto de color: verde lleno + "Trabajando" (animación de pulso suave) / gris + "Descansando". Reutiliza tokens `--color-success` / `--color-text-muted`.
- **C2:** Integrado dentro de la tarjeta del cronómetro (B1) como subtítulo.

### D. Resumen de jornadas de hoy (REDISEÑO)

- **D1 (recomendada):** **Fila de mini-tarjetas (stat cards)** debajo del botón: «Hoy» (total horas, formato `7h 30m`), «Jornadas» (nº), y opcionalmente «Restante» (objetivo − fichado) o «Pausa» (huecos del día). Cada una con etiqueta pequeña + valor grande.
- **D2:** **Lista compacta de las jornadas de hoy** (entrada–salida + duración por jornada), reutilizando un `JornadaCard` ligero, para ver de un vistazo lo fichado hoy y detectar si falta cerrar algo.
- **D3:** Combinación: stat cards (D1) + lista colapsable de jornadas de hoy (D2).

Criterio: D1 es lo mínimo con estilo; D2/D3 acercan la pantalla a un verdadero dashboard. La lista de jornadas de hoy es además el sitio natural para _ver_ el resultado del alta manual recién creada.

### E. Pequeñas funcionalidades / widgets (opcionales, "nice to have")

- **E1:** **Barra/anillo de progreso al objetivo diario** (relacionado con B2): "5h 20m de 8h" con barra.
- **E2:** **Saludo contextual** según la hora ("Buenos días" / "Buenas tardes"), tono cercano.
- **E3:** **Mini-indicador de la semana**: total fichado en la semana actual vs objetivo semanal (un solo número + barra), enlazando mentalmente con Estadísticas.
- **E4:** **Aviso de jornada sin cerrar** si quedó una jornada abierta de un día anterior (banner discreto con acción "revisar").
- **E5:** **Última jornada**: "Última salida: ayer 18:02".

Estas se priorizan en planificación; no todas entran en el primer corte.

## Historias de usuario

- Como usuario, quiero ver la **hora actual** en la pantalla principal, además de la fecha.
- Como usuario, quiero que el **cronómetro, el estado y el resumen de hoy** tengan un diseño cuidado y legible de un vistazo.
- Como usuario, quiero un **acceso discreto** para **añadir un fichaje a mano** cuando se me olvidó fichar.
- Como usuario, al añadir un fichaje a mano quiero indicar **fecha, hora de inicio y hora de fin** en una **modal**.
- Como usuario, quiero poder **seguir añadiendo más fichajes** sin cerrar la modal tras guardar uno.
- Como usuario, quiero que la jornada añadida a mano aparezca **inmediatamente** en el resumen/jornadas de hoy (si es de hoy) y en el Historial.
- Como usuario, quiero que el **botón principal de fichar entrada/salida** siga siendo lo más prominente de la pantalla.

## Criterios de aceptación

### Cabecera: fecha y hora

- [x] AC-01: La pantalla muestra la **fecha actual** en formato largo en español (como hoy).
- [x] AC-02: La pantalla muestra la **hora actual** (`HH:MM`), visualmente diferenciada del cronómetro.
- [x] AC-03: La hora se **actualiza** automáticamente (al menos cada minuto; segundos opcionales).
- [x] AC-04: La fecha sigue actualizándose al cambiar de día con la app abierta (no regresión de 003.2).

### Rediseño del cronómetro y estado

- [x] AC-05: El cronómetro se presenta dentro de un **contenedor con estilo** (tarjeta/anillo), manteniendo `HH:MM:SS`, `font-mono` y `tabular-nums`.
- [x] AC-06: El contenedor o un acento **refleja el estado** (Trabajando vs Descansando) mediante color.
- [x] AC-07: El indicador de estado es un **badge/pill** con punto de color (verde "Trabajando" / neutro "Descansando").
- [x] AC-08: El **botón Fichar entrada/salida** conserva su prominencia y comportamiento (no regresión de 003.2): texto y color reactivos al estado.

### Resumen del día con estilo

- [x] AC-09: El resumen de hoy se muestra con **estilo de stat cards** (al menos: total de horas de hoy y nº de jornadas).
- [x] AC-10: El total de horas usa la **duración efectiva/redondeada** vigente (coherente con Historial/Estadísticas) y formato `Xh Ym`.
- [x] AC-11: El resumen se **actualiza reactivamente** al fichar, parar o **crear una jornada manual** que caiga en hoy.

### Widget de contexto (E4) y scaffold

- [x] AC-12: **Aviso de jornada sin cerrar (E4)**: si existe una jornada `open` de un **día anterior** a hoy, se muestra un banner discreto con la fecha y una acción "Revisar". Si no hay ninguna, el banner no aparece.
- [x] AC-13: El banner E4 no interfiere con la jerarquía: queda subordinado al cronómetro y al botón principal.
- [x] AC-14: La stat card "Restante" (D1) muestra `objetivo diario − fichado hoy` cuando hay objetivo derivable (`horas_semanales / dias_laborables`); si no, degrada con elegancia (se omite o muestra "—").
- [x] AC-15: **Scaffold de widgets configurables**: la pantalla compone los bloques (cabecera, cronómetro, estado, resumen, widget de contexto) a partir de un **registro/lista de slots** estático, con componentes de **interfaz homogénea**, de forma que añadir/intercambiar una variante (p. ej. B2, E3) no requiera reescribir `+page.svelte`. No se implementa UI de configuración ni persistencia en esta spec.

### Acceso al alta manual

- [x] AC-16: Existe un control **secundario y discreto** (enlace/botón pequeño con icono "+", p. ej. "Añadir fichaje") para abrir la modal de alta manual.
- [x] AC-17: El control **no compite** visualmente con el botón principal (tamaño/jerarquía claramente menores).

### Modal de alta manual

- [x] AC-18: Al activar el control se abre una **modal** con campos: **fecha** (`date`), **hora de inicio** (`time`) y **hora de fin** (`time`).
- [x] AC-19: La modal es **accesible**: foco atrapado dentro, cierre con `Esc` y con botón "Cancelar"/"X", `aria-modal`/rol de diálogo, y overlay que la separa del fondo.
- [x] AC-20: La **fecha no puede ser futura** (`max` = hoy).
- [x] AC-21: La **hora de fin debe ser posterior a la de inicio** cuando la jornada es del mismo día; si no, se muestra error y no se guarda (salvo cruce de medianoche con la casilla, ver AC-30–AC-32).
- [x] AC-22: Se **valida la duración mínima** (`min_jornada_minutos`) si está activada: si la jornada no la alcanza, se **bloquea el guardado** y se muestra el error (mismo rigor que el fichaje normal).
- [x] AC-23: Al **Guardar**, se crea una jornada **cerrada** (`status: 'closed'`, `synced: 0`) con `start_time`/`end_time` derivados de fecha+horas y `duration` calculada en minutos.
- [x] AC-24: Botón **"Guardar y añadir otra"**: persiste la jornada y **deja la modal abierta**, lista para un nuevo alta (sin cerrar). Conveniente: precargar la fecha del alta anterior; limpiar las horas.
- [x] AC-25: Botón **"Guardar"** (a secas): persiste y **cierra** la modal.
- [x] AC-26: Tras guardar, la(s) jornada(s) creada(s) aparecen en el **resumen de hoy** (si son de hoy) y en el **Historial**, sin recargar.
- [x] AC-27: Feedback de éxito tras cada guardado (toast/mensaje "Fichaje añadido").

### Calidad

- [x] AC-28: `npm run check` y `npm run build` sin errores.
- [x] AC-29: Tests: utilidades de alta manual (construcción de `start/end`, validaciones) y, si procede, la página/modal.

### Cruce de medianoche y solapamiento

- [x] AC-30: La modal incluye una casilla **"Termina al día siguiente"** (desmarcada por defecto) para registrar jornadas que cruzan medianoche.
- [x] AC-31: Con la casilla **marcada**, la hora de fin se interpreta en el **día siguiente** (fecha de inicio + 1) y se guarda una jornada cerrada que cruza medianoche con la `duration` correcta.
- [x] AC-32: Con la casilla **desmarcada**, la hora de fin debe ser posterior a la de inicio (no hay cruce implícito); en caso contrario se bloquea con error.
- [x] AC-33: Antes de guardar, se detecta **solapamiento** con jornadas existentes; si la nueva jornada se solapa con alguna, se **bloquea el guardado** y se informa de la jornada en conflicto (hora de inicio/fin).

## Notas técnicas

### Estructura de archivos (propuesta)

```
src/
├── lib/
│   ├── components/
│   │   ├── AltaManualModal.svelte   # NUEVO: modal con form + encadenado
│   │   └── dashboard/               # NUEVO: bloques intercambiables (scaffold)
│   │       ├── CabeceraFechaHora.svelte   # A1
│   │       ├── CronometroCard.svelte       # B1 (acento por estado)
│   │       ├── EstadoPill.svelte           # C1
│   │       ├── ResumenStatCards.svelte     # D1
│   │       └── AvisoJornadaAbierta.svelte  # E4
│   └── utils/
│       ├── alta-manual.ts           # NUEVO: build de jornada + validaciones
│       └── dashboard-layout.ts      # NUEVO: registro/lista de slots (estático)
└── routes/
    └── +page.svelte                 # MODIFICAR: compone los slots del registro
```

### Scaffold de widgets configurables (AC-15)

Para dejar la pantalla preparada para la configurabilidad futura **sin implementarla**:

- Cada bloque vive en `src/lib/components/dashboard/` como componente con **props homogéneas** (recibe lo que necesite de `{ clockedIn, elapsed, jornadasHoy, resumen, settings }`), de modo que sean intercambiables sin tocar la página.
- `dashboard-layout.ts` exporta un **registro estático** que describe el orden y la variante de cada slot, p. ej.:

```typescript
type SlotId = 'cabecera' | 'cronometro' | 'estado' | 'resumen' | 'contexto';
type DashboardSlot = { id: SlotId; variante: string; visible: boolean };

// Configuración por defecto (única en esta spec); en el futuro la sustituirá
// una preferencia del usuario cargada de Settings/IndexedDB.
export const dashboardLayoutPorDefecto: DashboardSlot[] = [
	{ id: 'cabecera', variante: 'A1', visible: true },
	{ id: 'cronometro', variante: 'B1', visible: true },
	{ id: 'estado', variante: 'C1', visible: true },
	{ id: 'resumen', variante: 'D1', visible: true },
	{ id: 'contexto', variante: 'E4', visible: true }
];
```

- `+page.svelte` itera ese registro y mapea `variante → componente`. Añadir B2/E3 en el futuro = registrar el componente y exponerlo como variante elegible; **no** se reescribe la página.
- En esta spec el registro es fijo y no hay UI ni persistencia de preferencias (eso es una spec futura).

### Persistencia del alta manual

`createJornada()` en [`src/lib/db.ts`](../../src/lib/db.ts) solo crea jornadas **abiertas** con `start_time = now`. Para el alta manual hace falta insertar una jornada **cerrada** con tiempos explícitos. Opciones:

- Añadir `crearJornadaManual({ start, end })` en `db.ts` que calcule `duration = Math.floor((end - start)/60000)`, `status: 'closed'`, `synced: 0`, coords `null`.
- Exponer una acción en el store `app-state` (p. ej. `agregarJornadaManual`) que inserte y luego llame a `cargarJornadas()` para refrescar el estado reactivo (resumen de hoy, etc.).

El patrón de inserción ya está validado en los tests (`addJornadaCerrada` en `app-state.test.ts`); reutilizar esa forma.

### Validaciones (`alta-manual.ts`)

```typescript
// Combina fecha (Date a 00:00) + "HH:MM" en un Date local.
export function combinarFechaHora(fecha: Date, hora: string): Date;

// Devuelve null si OK, o un mensaje de error si inválido.
export function validarAltaManual(start: Date, end: Date, minJornadaMinutos: number): string | null;
// - end > start
// - no futura (start <= ahora)
// - (end - start) >= minJornadaMinutos si min > 0
```

- La **duración mostrada** en el resumen y el Historial debe respetar `redondeo_minutos` vía `duracionEfectivaMinutos` (no inventar otro cálculo).
- La **hora actual** de la cabecera puede formatearse con `Intl.DateTimeFormat('es-ES', { hour: '2-digit', minute: '2-digit' })`; añadir helper a `dashboard-format.ts` si conviene.

### Reactividad

- Tras crear la jornada manual, llamar a la recarga del store (`cargarJornadas()`), que ya alimenta `getJornadasHoy()` / `getResumenHoy()` usados por la home.
- El cronómetro/estado en curso **no** deben verse afectados por un alta manual (son cosas independientes).

### Accesibilidad de la modal

- Diálogo nativo `<dialog>` o patrón con `role="dialog"` + `aria-modal="true"`.
- Trampa de foco, restitución de foco al cerrar, cierre con `Esc` y clic en overlay.

## Edge cases

- **Hora de fin anterior o igual a inicio (mismo día)** → error, no guarda (AC-21). Para turnos nocturnos, el usuario marca "Termina al día siguiente" (AC-30–AC-31).
- **Jornada que cruza medianoche** → soportada vía la casilla "Termina al día siguiente": `end = fecha + 1` a la hora de fin (AC-31).
- **Fecha futura** → bloqueada por `max` y revalidada al guardar. Una salida resultante en el futuro (incluida la del día siguiente aún no transcurrido) también se bloquea.
- **Duración menor que `min_jornada_minutos`** → se **bloquea** el guardado y se muestra el error (decisión cerrada; alinear con 003.5). _Nota:_ el alta manual evalúa el mínimo con `settingsActual()` (config actual), a diferencia de la jornada en vivo (que usa el vigente al `start_time`, 003.5). **Decisión cerrada:** se deja así a propósito — un alta manual normalmente supera el mínimo, así que no compensa pasar `settingsVigente(start del alta)`.
- **Solapamiento con otra jornada existente** → se **bloquea** el guardado y se informa de la jornada en conflicto (AC-33). El solape se evalúa contra todas las jornadas; las abiertas ocupan `[inicio, ahora]`.
- **Alta de una jornada que NO es de hoy** → no aparece en el resumen de hoy, pero sí en Historial.
- **"Guardar y añadir otra" repetido** → cada guardado es independiente; mantener la fecha facilita cargar varias jornadas del mismo día.

## Verificación

| Criterio              | Método                               | Evidencia                                                  |
| --------------------- | ------------------------------------ | ---------------------------------------------------------- |
| Hora actual           | Abrir la home, esperar 1 min         | Hora visible y actualizándose, distinta del crono          |
| Cronómetro con estilo | Ver la pantalla en los dos estados   | Tarjeta/anillo con acento según Trabajando/Descansando     |
| Estado badge          | Iniciar/parar jornada                | Pill verde "Trabajando" / neutro "Descansando"             |
| Resumen con estilo    | Tener jornadas hoy                   | Stat cards con horas y nº de jornadas                      |
| Acceso discreto       | Mirar jerarquía visual               | "+ Añadir fichaje" claramente secundario                   |
| Modal campos          | Abrir la modal                       | Fecha, inicio, fin presentes y accesibles                  |
| Validación fin>inicio | Fin ≤ inicio y guardar               | Error, no se crea jornada                                  |
| Guardar               | Rellenar y Guardar                   | Modal se cierra, jornada en resumen/Historial              |
| Encadenar             | "Guardar y añadir otra" 2 veces      | Modal sigue abierta; 2 jornadas creadas                    |
| Cruce medianoche      | Marcar casilla, 23:00→02:00          | Jornada cerrada con fin del día siguiente y duración 3h    |
| Solapamiento          | Alta que pisa una jornada existente  | Bloqueado, aviso con la jornada en conflicto               |
| Reactividad           | Crear jornada manual de hoy          | Resumen de hoy se actualiza sin recargar                   |
| Aviso E4              | Dejar una jornada abierta de ayer    | Banner "Jornada sin cerrar" con acción Revisar             |
| Scaffold              | Revisar `+page.svelte` y el registro | La página itera el registro de slots; bloques desacoplados |
| Calidad               | `npm run check` && `npm run build`   | Sin errores                                                |

## Fuera de scope

- **UI de configuración del dashboard y persistencia de la preferencia** del usuario (intercambiar/reordenar widgets): en esta spec solo se deja el **scaffold** (registro estático). La personalización real es una spec futura.
- **Variantes alternativas** B2 (anillo de objetivo), B3, A2/A3, C2, D2/D3, E1/E2/E3/E5: documentadas como catálogo, no se implementan aquí.
- **Edición y eliminación** de jornadas existentes (alta sí, edición no) → spec aparte.
- **Geolocalización** en el alta manual (las coords quedan `null`).
- **Sincronización** con backend (spec 004) y el icono de sync.
- **Generación de exportación** (spec aparte, ver 003.7).
